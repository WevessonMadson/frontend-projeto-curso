import  "./Header.css";

const Header = () => {
   return (
      <header className={"header"}>
         <h1>Cadastro de Pessoas</h1>
         <span>(React + NodeJs)</span>
      </header>
   );
};

export default Header;
